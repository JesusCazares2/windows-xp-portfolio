---
import homeScreen from '../../assets/fullertonfunhouse-images/homeScreenFF.png';
import brickBreaker from '../../assets/fullertonfunhouse-images/bb.png';
import blackJack from '../../assets/fullertonfunhouse-images/BJ.png';
import ZMaze from '../../assets/fullertonfunhouse-images/zMaze.png';
import flappyBird from '../../assets/fullertonfunhouse-images/FB.png';
import slotMachine from '../../assets/fullertonfunhouse-images/SM.png';
import spaceShooter from '../../assets/fullertonfunhouse-images/spaceShooter.png';
---
<div id="ff-project-wrapper">
<div class="project-item">
  <h4>Fullerton Funhouse</h4>
  <p>Architected a full-stack web platform featuring 6 interactive titles, utilizing Python (Flask) and JavaScript to manage game states and dynamic frontend interactivity.</p>
  <p>Implemented a unified UI/UX framework with responsive CSS and HTML5, ensuring 100% platform parity across desktop browsers.</p>
  <p>Optimized asset loading and site navigation by building a centralized interface, reducing page load latency.</p>
  <a href="https://fullertonfunhouse.onrender.com/" target="_blank" rel="noopener noreferrer">Launch Live Demo 🚀</a>
  <ul>
    <li>Tech: Python, JavaScript, HTML, CSS, Flask</li>
  </ul>
</div>

<div class="image-container">
  <div class="mySlides fade">
    <img class="bg-blur" src={homeScreen.src} aria-hidden="true" />
    <img class="main-img" src={homeScreen.src} alt="Fullerton Funhouse Home Screen" />
  </div>

  <div class="mySlides fade">
    <img class="bg-blur" src={brickBreaker.src} aria-hidden="true" />
    <img class="main-img" src={brickBreaker.src} alt="Brick Breaker Game" />
  </div>

  <div class="mySlides fade">
    <img class="bg-blur" src={blackJack.src} aria-hidden="true" />
    <img class="main-img" src={blackJack.src} alt="Black Jack Game" />
  </div>

  <div class="mySlides fade">
    <img class="bg-blur" src={ZMaze.src} aria-hidden="true" />
    <img class="main-img" src={ZMaze.src} alt="Z Maze Game" />
  </div>

  <div class="mySlides fade">
    <img class="bg-blur" src={flappyBird.src} aria-hidden="true" />
    <img class="main-img" src={flappyBird.src} alt="Flappy Bird Game" />
  </div>

  <div class="mySlides fade">
    <img class="bg-blur" src={slotMachine.src} aria-hidden="true" />
    <img class="main-img" src={slotMachine.src} alt="Slot Machine Game" />
  </div>

  <div class="mySlides fade">
    <img class="bg-blur" src={spaceShooter.src} aria-hidden="true" />
    <img class="main-img" src={spaceShooter.src} alt="Space Shooter Game" />
  </div>

  <a class="next" id="ffNextSlide">&#10095;</a>
  <a class="prev" id="ffPrevSlide">&#10094;</a>
</div>

<br> 

<div style="text-align:center">
  <span class="dot"></span>
  <span class="dot"></span>
  <span class="dot"></span>
  <span class="dot"></span>
  <span class="dot"></span>
  <span class="dot"></span>
  <span class="dot"></span>
</div>
</div>

<style>
  .project-item ul {
    list-style-type: none;
    padding-left: 5px;
    margin-top: 10px;
  }
  .project-item li {
    position: relative;
    padding-left: 20px;
    margin-bottom: 10px;
    line-height: 1.5;
    font-size: 15px;
  }

  .project-item li::before {
    content: "";
    position: absolute;
    left: 0;
    top: 6px;
    width: 8px;
    height: 8px;
    background: linear-gradient(135deg, #0058e6 0%, #3a93ff 100%);
    border: 1px solid #003399;
    box-shadow: 1px 1px 1px rgba(0,0,0,0.2);
  }

  .image-container {
    max-width: 500px;
    height: 300px;  
    position: relative;
    margin: auto;
    border: 1px solid #7f9db9;
    background: #000;
    overflow: hidden;
  }

  .mySlides {
    display: none;
    width: 100%;
    height: 100%;
    position: relative;
  }

  .bg-blur {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    object-fit: cover;
    filter: blur(12px) brightness(0.6);
    transform: scale(1.1); 
  }

  .main-img {
    position: relative;
    width: 100%;
    height: 100%;
    object-fit: contain; 
    z-index: 1;
  }

  .prev, .next {
    cursor: pointer;
    position: absolute;
    top: 50%;
    width: auto;
    padding: 16px;
    margin-top: -22px;
    color: white;
    text-shadow: 0 0 5px rgba(0, 0, 0, 0.8);
    font-weight: bold;
    font-size: 18px;
    transition: 0.6s ease;
    border-radius: 0 3px 3px 0;
    user-select: none;
    z-index: 10;
  }

  .next {
    right: 0;
    border-radius: 3px 0 0 3px;
  }

  .prev:hover, .next:hover {
    background-color: rgba(0, 0, 0, 0.8);
  }

  .dot {
    cursor: pointer;
    height: 15px;
    width: 15px;
    margin: 0 2px;
    background-color: #bbb;
    border-radius: 50%;
    display: inline-block;
    transition: background-color 0.6s ease;
  }

  .dot:global(.active), .dot:hover {
    background-color: #717171;
  }

  .fade {
    animation-name: fade;
    animation-duration: 1.5s;
  }

  @keyframes fade {
    from {opacity: .4}
    to {opacity: 1}
  }
</style>

<script>
  const wrapper = document.getElementById("ff-project-wrapper");
  const slides = wrapper?.querySelectorAll(".mySlides") as NodeListOf<HTMLElement>;
  const dots = wrapper?.querySelectorAll(".dot") as NodeListOf<HTMLElement>;
  let slideIndex = 1;

  function showSlides(n) {
    if (!slides || !dots) return;
    if (n > slides.length) slideIndex = 1;
    if (n < 1) slideIndex = slides.length;

    slides.forEach(slide => (slide.style.display = "none"));
    dots.forEach(dot => dot.classList.remove("active"));

    if (slides[slideIndex - 1]) slides[slideIndex - 1].style.display = "block";
    if (dots[slideIndex - 1]) dots[slideIndex - 1].classList.add("active");
  }

  showSlides(slideIndex);

  wrapper?.querySelector("#ffPrevSlide")?.addEventListener("click", () => showSlides(--slideIndex));
  wrapper?.querySelector("#ffNextSlide")?.addEventListener("click", () => showSlides(++slideIndex));

  dots.forEach((dot, index) => {
    dot.addEventListener("click", () => {
      slideIndex = index + 1;
      showSlides(slideIndex);
    });
  });
</script>
